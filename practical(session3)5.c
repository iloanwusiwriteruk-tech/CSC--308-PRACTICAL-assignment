Observation

With a counting semaphore:

Three threads execute together.
Waiting time is reduced.
Better resource utilization is achieved.

With a mutex:

Only one thread executes at a time.
More waiting occurs.
Lower parallelism.